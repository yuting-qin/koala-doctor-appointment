spring.application.name=koala-doctor-appointment

# Render Spring's framework-level exceptions (missing/malformed query params, validation,
# 405, 415, etc.) as RFC 7807 ProblemDetail bodies — same shape as our app-level errors.
spring.mvc.problemdetails.enabled=true
